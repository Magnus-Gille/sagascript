# 1.2.0 preparation evidence

Status: locally verified preparation, **not approved for publication**.

## Scope and acceptance

Based on owner-tested `e4db9c6137b73c710826a21f5792ab5d31c4a798`.
The existing Bluetooth microphone website warning from PR #219 is preserved.
Concurrent R6 meeting-review work and ASR research are not integrated here.

The owner reports Windows testing complete and confirmed Snapdragon on
2026-09-08: ARM64 is the accepted architecture for the stable release. Windows
x64 remains beta; this acceptance does not cover Intel/AMD machines.
Swedish/English shortcut acceptance was reported by the owner. Finnish relies on
model evidence; no native-speaker acceptance or measured improvement is claimed.

Candidate packages report 1.1.3. Fresh 1.2.0 builds, checksums, macOS
signing/notarization and final artifact smoke tests remain required. Explicit
publication and deployment approval must identify the final immutable revision.

## Completed checks

- Version consistency and dependency-license check pass.
- Rust workspace check, 885 tests, clippy with warnings denied, and lean CLI build pass.
- Optional meeting feature: six CLI meeting tests pass with no default features.
- New default CLI exclusion test failed before the feature gate and passes after it.
- Frontend: 139 pass, one Windows-only skip; Svelte check and production build pass.
- Website build passes; isolated Chromium desktop 1280px and mobile 390px screenshots
  inspected. The later Windows link-target correction does not change layout.
- Tracked lockfiles used for application npm dependencies; no dependency changes.

Rust tests needed approved native access to a named test pasteboard; the initial
sandbox-only pasteboard failure disappeared with that access. No personal browser
profile or installed Sagascript app was used for these checks.

## Independent review and disposition

Reviewer: Anthropic `claude-opus-5`, read-only scoped patch review with no tools or
session persistence. Verdict: merge as release preparation; do not publish.

Grounded documentation findings addressed: Windows checksum attachment naming,
PowerShell verification, persistent architecture-specific acceptance checklist,
diarized-file smoke step, a distinct 1.2.0 beta tag, accurate wording for the
frontend branch gate, and a Windows link to the release list containing both
stable and beta channels. Installation documentation updated consistently.
Release notes are included as a tracked file, resolving the untracked-file concern.

Other review hypotheses checked:

- `transcribe_file` still honours diarization, formats `[speaker] text`, and returns
  that string to the existing result textarea (`commands.rs` and `Settings.svelte`).
  This source inspection is not end-to-end audio acceptance; final smoke testing
  explicitly includes this restored path.
- Production workflow build commands do not enable `meeting-mode` or
  `--all-features`. The optional feature deliberately enables future CLI work;
  it is prohibited for 1.2.0 artifacts. Default workspace clippy used
  `--all-targets`, not `--all-features`.
- The macOS verifier takes explicit app and DMG paths before draft creation.
  Windows artifacts have their own architecture-specific checksum files.
- Shortcut and dictionary features are in the pinned ancestry (PRs #215/#214).
  Finnish Base and optional FinnishNLP Tiny are in the pinned settings model map.
- `begin_meeting_file` is the only meeting-job creation command in the shell.
- Kept “next release” for meeting mode: this is the owner's explicit scope decision.
- Kept the inherited microphone-note appearance after visual inspection; no styling
  change is needed for this release.

M5 assistance: `qwen3-30b-instruct`, usefulness **partial**. Used the release-summary
structure; removed unsupported Finnish quality gains and overly broad privacy copy.
